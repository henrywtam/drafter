<h2>Drafting...</h2>

<% @pack.cards.each do |card| %>
  <a href="/drafts/<%= @draft.id %>"><img data-card="<%= card.id %>" data-pack="<%= @pack.id %>" data-round="<%= @round.id %>" data-draft="<%= @draft.id %>" class="draft-card" src="<%= card.image_url %>" /></a>
<% end %>